# Copy to apps/web/.env on the server BEFORE building the web app.
# These are baked into the static build, so set them before `pnpm --filter @msva/web build`.
VITE_API_BASE_URL=https://msva.maplestudios.co.in
VITE_TELEPHONY_WS_URL=wss://msva.maplestudios.co.in
