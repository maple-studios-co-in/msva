// VitePress theme entry — extends the default theme with our Madhusudan
// brand stylesheet. Adding pages, components, or layouts? Do it here.

import DefaultTheme from "vitepress/theme";
import "./custom.css";

export default DefaultTheme;
